# Create a simple sentence to start with

sentence = "I am Groot!"
print(f"Starting sentence: {sentence}")

# Use the split function to split the string at every space, then create a list of the resulting elements

print(f"New sentence: {sentence.split(' ')[1][0].upper()} {sentence.split(' ')[2][1:]}")



